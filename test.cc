#include <emscripten/emscripten.h>

EMSCRIPTEN_KEEPALIVE int doSubTest1(int a, int b)
{
  return a + b;
}

EMSCRIPTEN_KEEPALIVE int doSubTest2(int a, int b)
{
  return a + b;
}

EMSCRIPTEN_KEEPALIVE int doSubTest3(int a, int b)
{
  return a + b;
}

EMSCRIPTEN_KEEPALIVE int doSubTest4(int a, int b)
{
  return a + b;
}